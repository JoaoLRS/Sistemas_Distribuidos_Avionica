# CDI-01: Painel inicial 

## Uma tela de de inicialização do sistema da aeronave, deve aonde o usuari tera acesso a todo o fluxo do sistema.

1. O usuario acessa `/painel` a onde é mostrado para ele a tela inicial do sistema.
2. No `painel` conterar os seguites cards na lateral esquerdo:
    2.1 - `Temperatura do motor`:
    2.2 - Preção:
    2.3 - Start do motor: 
    2.4 - 